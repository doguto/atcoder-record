#include "bits/stdc++.h"

using namespace std;

// N <= 5*10^3
// => O(N^2 * logN) くらいまではok

int main() {
    int N;
    cin >> N;

    vector<int> D(N);
    vector<int> C(N);
    vector<int> S(N);
}
